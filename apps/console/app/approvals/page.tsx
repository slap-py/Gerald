import SectionPage from '../components/section-page';

export default function ApprovalsPage() {
  return (
    <SectionPage
      title="Approvals"
      description="Protected writes and ambiguous outcomes will appear here when later phases enable them."
    />
  );
}
