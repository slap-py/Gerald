import SectionPage from '../components/section-page';

export default function SecurityPage() {
  return (
    <SectionPage
      title="Security"
      description="Manage Normal, Read-only, and Locked modes, passkeys, PIN policy, recovery, and sessions."
    />
  );
}
