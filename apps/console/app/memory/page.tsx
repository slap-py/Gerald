import SectionPage from '../components/section-page';

export default function MemoryPage() {
  return (
    <SectionPage
      title="Memory"
      description="Review profile facts, episodes, entities, decisions, provenance, and forgetting controls."
    />
  );
}
