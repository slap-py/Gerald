import SectionPage from '../components/section-page';

export default function TasksPage() {
  return (
    <SectionPage
      title="Tasks"
      description="Inspect the active task tree and immutable task transitions."
    />
  );
}
