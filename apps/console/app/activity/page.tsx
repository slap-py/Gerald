import SectionPage from '../components/section-page';

export default function ActivityPage() {
  return (
    <SectionPage
      title="Activity"
      description="Reconstruct runs, policy decisions, tool calls, and deliveries from the audit trail."
    />
  );
}
