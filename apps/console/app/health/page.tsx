import SectionPage from '../components/section-page';

export default function HealthPage() {
  return (
    <SectionPage
      title="Health"
      description="Check API readiness, worker connectivity, connector status, and migration state."
    />
  );
}
