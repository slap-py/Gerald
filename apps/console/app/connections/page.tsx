import SectionPage from '../components/section-page';

export default function ConnectionsPage() {
  return (
    <SectionPage
      title="Connections"
      description="Connect Google with read-only Gmail, Calendar, Drive, and Contacts scopes."
    />
  );
}
